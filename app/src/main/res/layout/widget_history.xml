<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/widget_history.xml — 3×2 cell, ~270×180dp -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="8dp"
    android:background="@drawable/widget_bg_rounded">

    <!--
        The bitmap is drawn at 660×360px (3:2 ratio) and fitted without
        distortion using fitCenter — this fixes the vertical stretch.
    -->
    <ImageView
        android:id="@+id/widget_history_canvas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true" />

    <TextView
        android:id="@+id/widget_history_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="Routine" />

</LinearLayout>